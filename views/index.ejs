<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/index.css">
    <script src="/js/jquery.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <title>Kittis</title>
</head>
<body>
    <h1>Welcome to the world of kittis</h2>
    <!--we have the whole object of json in kittiData, so we can directly access any key of kittiData -->
    <!--kittiData.lulu.name -> write this in ejs format -->

    <!--We cannot directly loop over the object, so we will first create array of all keys and then iterate over it using loop -->
    <!-- for(let cat of Object.keys(kittiData)){ 
        kittiData[cat].title  
        <div>
        for(let bf of kittiData[cat].bestFriend){ 
            bf.name  
        }
        </div>
    }
    -->
    <div class="container">
        <% for(let cat of Object.keys(kittiData)){ %>
            <div class="single">
                <a href="/cats/<%= kittiData[cat].name.toLowerCase() %>"><h5><%= kittiData[cat].name %></h3></a>
                <img class="hover" src="<%= kittiData[cat].img %>" alt="">
            </div>
        <% } %>
    </div>
    <div class="newCat">
      <a href="/cats/new">New Cat</a>
    </div>
    

</body>
</html>